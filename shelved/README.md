# Shelved Subsystems

These are specs for subsystems that were removed during simplification. They are NOT planned features.

They document capabilities that existed but were removed because they were speculative, disabled by default, and added complexity that made the codebase harder to reason about.

They may be reconsidered if there is measured evidence they would provide real benefit. Each spec describes what would need to be true for that to happen.
